name: R workflow
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
   # - name: Set up R
    #  uses: r-lib/actions/setup-r@v1
    - name: Install dependencies
      uses: r-lib/actions/setup-r-dependencies@f57f1301a053485946083d7a45022b278929a78a
      with:
        packages: |
          jsonlite
    - name: R CMD check
      run: |
       #  R CMD build .
       # R CMD check *tar.gz
       installed.packages()
      shell: Rscript {0}
